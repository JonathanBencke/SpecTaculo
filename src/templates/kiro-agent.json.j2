{% from '_body.md.j2' import body %}
{
  "name": "{{ name }}",
  "description": {{ description | replace('\n', ' ') | tojson_unicode }},
  "mode": {{ mode | tojson_unicode }},
  "systemPrompt": {{ body(name, entry, output, rules, prompt, tool) | tojson_unicode }}
}
